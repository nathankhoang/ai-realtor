// Twitter card uses the same image as the Open Graph card. Re-exporting
// from opengraph-image keeps both in sync without duplicating the JSX.
export { default, runtime, size, contentType, alt } from './opengraph-image'
